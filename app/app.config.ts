export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'neutral'
    },
    navigationMenu: {
      slots: {
        label: "font-semibold text-xs/5 text-highlighted"
      }
    }
  }
})
